{
  "name": "rf_combinator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bun build --compile --minify --sourcemap --bytecode ./index.js --outfile rf_combinator",
    "build-win32": "bun build --compile --target=bun-windows-x64 --minify --sourcemap --bytecode ./index.js --outfile rf_combinator.exe",
    "build-linux": "bun build --compile --target=bun-linux-x64 --minify --sourcemap --bytecode ./index.js --outfile rf_combinator"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^5.1.0",
    "express-fileupload": "^1.5.1",
    "rf_shared": "file:../rf_shared"
  }
}
